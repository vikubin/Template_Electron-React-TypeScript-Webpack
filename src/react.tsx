import * as React from 'react';
import * as ReactDOM from 'react-dom';

const Index = () => {
  return <div>You have loaded the template. Good Job.</div>;
};

ReactDOM.render(<Index/>, document.getElementById('app'));